module.exports = {
  plugins: [
    <%_ if (config.cssPreprocessor === 'TailwindCSS') { _%>
    require('tailwindcss'),
    require('autoprefixer'),
    <%_ } else { _%>
    require('autoprefixer'),
    <%_ } _%>
  ]
} 